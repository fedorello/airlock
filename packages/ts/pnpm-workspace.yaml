# pnpm 11 reads build-script approvals from here (no longer the package.json
# "pnpm" field). esbuild needs its install script to fetch the platform binary
# used by Vitest, tsup, and tsx.
allowBuilds:
  esbuild: true
